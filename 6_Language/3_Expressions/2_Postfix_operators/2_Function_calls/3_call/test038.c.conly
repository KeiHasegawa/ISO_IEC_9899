#include <stdio.h>

double g(a,b,c,d,e,f)
     char a;
     unsigned char b;
     short int c;
     unsigned short int d;
     int e;
     unsigned int f;
{
  return (a * b - c) * (d * e + f);
}

int main(void)
{
  printf("%lf\n",g(1,2,3,4,5,6));
  return 0;
}
